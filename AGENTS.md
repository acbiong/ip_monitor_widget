# 本项目协作要求

- 遵守 `docs/AI_PROGRAMMING_STANDARD.md`，代码注释与文档使用中文。
- 每次用户提出一项修改任务，先从当前已交付提交建立独立 `feature/`、`fix/` 或 `docs/` 分支；同一任务的实现、修复和验证可在该分支分多次提交。
- 任务结束前提交该任务的源码、文档、版本状态与测试报告；不自动合并 main，不修改远程仓库，不强制推送。
- 不提交 dist、虚拟环境、缓存、临时日志或敏感配置；保留 PyInstaller spec、锁定依赖及必要资源。
- 功能增加递增 MINOR，兼容修复递增 PATCH，不兼容变更递增 MAJOR；每次构建由脚本递增后缀。
- 保持托盘入口、主窗口置底、网卡独立查询、设置预览/回滚等现有行为。关于页面不得请求公网或修改用户配置。
- 构建快照必须如实记录 Git 信息，不得通过过滤源码文件伪造干净状态；编译后的报告提交可以晚于包内源码提交。
- 更新接口文档、变更履历、测试记录和 tables 文件清单；`.git/` 内部对象及忽略的临时文件无需展开。
